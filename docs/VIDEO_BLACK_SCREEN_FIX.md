# Исправление проблемы "черного экрана" при воспроизведении видео

## 🔥 Проблема

Видео скачивается и отправляется в Telegram, но при воспроизведении показывается только **черный экран** без изображения.

## 🤔 Возможные причины

1. **Отсутствие метаданных видео** - Telegram не знает длительность, разрешение
2. **Неправильный формат видео** - Telegram требует определённые кодеки
3. **Видео не полностью скачалось** - повреждённый файл
4. **Отсутствие аудио дорожки** - хотя видео должно скачиваться с аудио

## ✅ Решение

### Добавлены метаданные видео при отправке

Теперь при отправке видео в Telegram передаются:

1. **Duration (длительность)** - обязательный параметр
2. **Width (ширина)** - опционально, но рекомендуется
3. **Height (высота)** - опционально, но рекомендуется  
4. **supports_streaming** - включено для лучшей совместимости

### Изменения в коде

**Новая функция `probe_video_metadata()`:**
```rust
/// Получает метаданные видео: длительность, ширину и высоту
/// Используется для корректной отправки видео в Telegram
fn probe_video_metadata(path: &str) -> Option<(u32, Option<u32>, Option<u32>)> {
    // Получает duration, width, height через ffprobe
}
```

**Обновлённая функция `send_video_with_retry()`:**
```rust
// Получаем метаданные перед отправкой
let video_metadata = probe_video_metadata(download_path);

let duration = video_metadata.map(|(d, _, _)| d);
let width = video_metadata.and_then(|(_, w, _)| w);
let height = video_metadata.and_then(|(_, _, h)| h);

// Отправляем с метаданными
bot.send_video(chat_id, InputFile::file(path))
    .duration(duration)
    .width(width)
    .height(height)
    .supports_streaming(true)
    .await
```

## 🛠️ Требования

Для работы нужен **ffprobe** (часть ffmpeg):

```bash
# Проверьте установлен ли ffprobe
ffprobe -version

# Если нет, установите ffmpeg
# macOS
brew install ffmpeg

# Ubuntu/Debian
sudo apt-get install ffmpeg

# Arch
sudo pacman -S ffmpeg
```

## 📊 Что изменилось

**ДО (проблема):**
```rust
bot.send_video(chat_id, InputFile::file(path))
    .await
```

**ПОСЛЕ (исправлено):**
```rust
let metadata = probe_video_metadata(path);
bot.send_video(chat_id, InputFile::file(path))
    .duration(metadata.duration)
    .width(metadata.width)
    .height(metadata.height)
    .supports_streaming(true)
    .await
```

## 🎯 Проверка

После перезапуска бота:

1. **Скачайте видео через бота**
2. **Проверьте логи:**
```bash
grep "Video metadata" /path/to/logs
```

Должны увидеть:
```
[INFO] Video metadata for /path/to/video.mp4: Some((180, Some(1280), Some(720)))
```
Где:
- `180` - длительность в секундах
- `1280` - ширина в пикселях
- `720` - высота в пикселях

3. **Откройте видео в Telegram** - должно воспроизводиться корректно

## 🔍 Диагностика

### Если проблема сохраняется

1. **Проверьте, что видео действительно скачалось:**
```bash
# Проверьте файл локально
ffprobe /path/to/downloads/video.mp4
```

Должны увидеть информацию о видео и аудио дорожках:
```
Stream #0:0: Video: h264 ...
Stream #0:1: Audio: aac ...
```

2. **Проверьте формат видео:**
```bash
ffprobe -v error -show_format -show_streams /path/to/downloads/video.mp4
```

Telegram предпочитает:
- ✅ **Видео:** H.264 (h264)
- ✅ **Аудио:** AAC (aac)
- ✅ **Контейнер:** MP4

Если формат другой, можно добавить перекодирование:
```rust
// В yt-dlp аргументах
"--postprocessor-args", "ffmpeg:-c:v libx264 -c:a aac"
```

3. **Проверьте размер файла:**
```bash
ls -lh /path/to/downloads/video.mp4
```

Если файл очень маленький (< 1 MB для видео) - возможно, скачалось только аудио или видео повреждено.

4. **Проверьте, что yt-dlp правильно объединяет видео и аудио:**
```bash
# Запустите с отладкой
RUST_LOG=doradura=debug ./target/release/doradura
```

В логах должно быть:
```
[DEBUG] yt-dlp stdout: [Merger] Merging formats into ...
```

## 🚀 Дополнительные улучшения (если нужно)

### Если проблема с форматом кодека

Можно добавить принудительное перекодирование:

```rust
// В download_video_file_with_progress
args.extend_from_slice(&[
    "--postprocessor-args", "ffmpeg:-c:v libx264 -preset fast -crf 23 -c:a aac -b:a 128k",
    "--no-check-certificate",
    &url_str,
]);
```

Но обычно это не нужно - yt-dlp скачивает совместимые форматы.

### Если нужно проверить наличие аудио

```rust
// Можно добавить проверку через ffprobe
fn has_audio_stream(path: &str) -> bool {
    let output = Command::new("ffprobe")
        .args([
            "-v", "error",
            "-select_streams", "a:0",
            "-show_entries", "stream=codec_name",
            "-of", "default=noprint_wrappers=1:nokey=1",
            path,
        ])
        .output()
        .ok()?;
    
    !String::from_utf8_lossy(&output.stdout).trim().is_empty()
}
```

## ✅ Итог

- ✅ Добавлено получение метаданных видео (duration, width, height)
- ✅ Метаданные передаются в Telegram API
- ✅ Включена поддержка streaming
- ✅ Добавлено логирование для диагностики

**После перезапуска бота видео должны воспроизводиться корректно!** 🎉

## 📚 Ссылки

- **Telegram Bot API - sendVideo:** https://core.telegram.org/bots/api#sendvideo
- **ffprobe документация:** https://ffmpeg.org/ffprobe.html
- **yt-dlp формат видео:** https://github.com/yt-dlp/yt-dlp#format-selection

