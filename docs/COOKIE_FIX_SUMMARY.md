# ✅ Исправление ошибки "Sign in to confirm you're not a bot"

## 🎯 Что было изменено:

### 1. **Обновлен default для cookies** (`src/config.rs`)
   - **Было:** `YTDL_COOKIES_BROWSER` по умолчанию пустая строка (cookies не используются)
   - **Стало:** `YTDL_COOKIES_BROWSER` по умолчанию "chrome" (автоматически использует cookies из Chrome)

### 2. **Улучшено логирование** (`src/downloader.rs`)
   - Добавлены предупреждения при старте, если cookies не настроены
   - Логи теперь покажут, какой браузер/файл используется для cookies

### 3. **ИСПРАВЛЕНО: Android client + cookies конфликт** (`src/downloader.rs`)
   - **Проблема:** Android client не поддерживает cookies, из-за чего видео не скачивались
   - **Решение:** Динамический выбор клиента в зависимости от наличия cookies:
     - **С cookies:** `web,ios,tv_embedded` (все поддерживают cookies)
     - **Без cookies:** `android,web` (fallback)
   - Исправлено для: metadata, audio download, video download

### 4. **Обновлена документация** (`FIX_YOUTUBE_ERRORS.md`)
   - Добавлен раздел "Quick Fix" в начале документа
   - Четкие инструкции для исправления ошибки bot detection

### 5. **Обновлен yt-dlp** 
   - **Было:** версия 2025.06.30 (устаревшая)
   - **Стало:** версия 2025.10.22 (последняя)
   - Исправлена ошибка "Signature extraction failed"

### 6. **Улучшены сообщения об ошибках** (`src/downloader.rs`)
   - Добавлена функция `send_error_with_sticker_and_message`
   - Теперь бот распознает тип ошибки и показывает понятное объяснение:
     - **"Only images available"** → объясняет, что видео недоступно (удалено/приватное/региональные ограничения)
     - **"Signature extraction failed"** → объясняет, что версия устарела
     - **"Bot detection"** → объясняет, что нужны cookies
   - Вместо общего "все сломалось" пользователь получает конкретные причины и решения

### 7. **Улучшены warning сообщения при старте** (`src/downloader.rs`)
   - При запуске без cookies бот теперь показывает **детальное предупреждение**
   - **На macOS:** Специальное сообщение с инструкцией по экспорту cookies в файл
   - **На Linux/Windows:** Инструкция по установке зависимостей и настройке браузера
   - Предупреждение показывается сразу, а не после первой ошибки

### 8. **Изменен default для cookies** (`src/config.rs`)
   - **Было:** `chrome` (не работает на macOS без Full Disk Access)
   - **Стало:** пустая строка (показывает предупреждение и направляет к MACOS_COOKIES_FIX.md)
   - На macOS рекомендуется использовать файл cookies вместо автоматического извлечения

---

## ⚠️ ВАЖНО ДЛЯ macOS:

На macOS **Chrome и Safari требуют специальных прав** для доступа к cookies, поэтому автоматическое извлечение может не работать.

**Симптомы:**
- Видишь: `Extracted 0 cookies from chrome (8136 could not be decrypted)`
- Ошибка: `cannot decrypt v10 cookies: no key found`
- Результат: "Only images available" вместо видео

**Решение:** Используй файл cookies вместо автоматического извлечения → **См. [MACOS_COOKIES_FIX.md](MACOS_COOKIES_FIX.md)**

---

## 🚀 Что нужно сделать СЕЙЧАС:

### Вариант 1: Файл cookies (РЕКОМЕНДУЕТСЯ ДЛЯ macOS)

```bash
# 1. Установи расширение для Chrome
# https://chrome.google.com/webstore/detail/get-cookiestxt-locally/cclelndahbckbenkjhflpdbgdldlbecc

# 2. Открой YouTube и залогинься
open https://youtube.com

# 3. Нажми на иконку расширения → Export → сохрани как youtube_cookies.txt

# 4. Скопируй файл в директорию проекта
mv ~/Downloads/youtube.com_cookies.txt ~/Dev/_PROJ/doradura/youtube_cookies.txt
chmod 600 ~/Dev/_PROJ/doradura/youtube_cookies.txt

# 5. Запусти бота со скриптом
cd ~/Dev/_PROJ/doradura
./run_with_cookies.sh
```

**Подробная инструкция:** См. [MACOS_COOKIES_FIX.md](MACOS_COOKIES_FIX.md)

---

### Вариант 2: Для Linux (автоматическое извлечение работает)

На Linux автоматическое извлечение cookies обычно работает:

```bash
# 1. Установи зависимости
pip3 install keyring pycryptodomex

# 2. Залогинься на YouTube в браузере

# 3. Запусти бота
export YTDL_COOKIES_BROWSER=chrome
cargo run --release
```

---

## 🔍 Проверка настройки:

```bash
# Проверь, что yt-dlp может читать cookies из Chrome
yt-dlp --cookies-from-browser chrome --print "%(title)s" "https://www.youtube.com/watch?v=dQw4w9WgXcQ"

# Или используй готовый скрипт
./check_cookies.sh chrome
```

Если команда выводит название видео - **все работает!** ✅

---

## 📋 Что происходит теперь:

1. **При старте бота** он автоматически попытается использовать cookies из Chrome
2. **В логах** ты увидишь:
   ```
   [INFO] Using cookies from browser: chrome
   ```
   Или предупреждение, если cookies не настроены:
   ```
   [WARN] No cookies configured! YouTube downloads may fail with 'bot detection' errors.
   ```

3. **При скачивании** yt-dlp будет использовать твои cookies для обхода bot detection

---

## ⚠️ Важно:

- **Cookies устаревают через 2-4 недели** - нужно будет заново залогиниться в YouTube
- **Если используешь Chrome/Chromium** - обязательно установи `keyring` и `pycryptodomex`
- **Safari работает лучше всего на macOS** - не требует дополнительных библиотек

---

## 🐛 Если все еще не работает:

1. **Проверь логи бота:**
   ```bash
   cargo run --release 2>&1 | grep -i cookie
   ```

2. **Попробуй другой браузер:**
   ```bash
   export YTDL_COOKIES_BROWSER=firefox
   cargo run --release
   ```

3. **Обнови yt-dlp:**
   ```bash
   ./update_ytdlp.sh
   ```

4. **Спроси в чате проекта** - приложи логи

---

## 📚 Дополнительная информация:

- [YOUTUBE_COOKIES.md](YOUTUBE_COOKIES.md) - Подробная документация по cookies
- [FIX_YOUTUBE_ERRORS.md](FIX_YOUTUBE_ERRORS.md) - Все исправления YouTube ошибок
- [QUICKSTART.md](QUICKSTART.md) - Быстрый старт проекта

---

**Готово!** 🎉 Теперь бот должен автоматически использовать cookies из Chrome при скачивании с YouTube.

