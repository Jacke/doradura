# 🔧 Исправление ошибок YouTube

## 🚨 Видишь ошибку "Sign in to confirm you're not a bot"?

**Это самая частая проблема!** Исправляется за 3 минуты:

### ⚡ Быстрое решение:

```bash
# 1. Установи Python зависимости
pip3 install keyring pycryptodomex --break-system-packages

# 2. Открой Chrome и залогинься на youtube.com

# 3. Перезапусти бота
cargo run --release
```

**Готово!** ✅ Бот теперь использует cookies из Chrome автоматически.

---

## Проблемы, которые мы исправили:

### 1. ❌ "Signature extraction failed"
### 2. ❌ "The following content is not available on this app"
### 3. ❌ "Sign in to confirm you're not a bot"

---

## ✅ Альтернативное исправление (через cookies файл):

```bash
# 1. Обновить yt-dlp
./update_ytdlp.sh

# 2. Пересобрать бота
cargo build --release

# 3. Запустить с cookies
./run_with_cookies.sh
```

---

## Что было сделано:

### 🔧 Изменения в коде:

1. **Добавлен Android клиент для обхода блокировок YouTube**
   - Параметр: `--extractor-args "youtube:player_client=android,web"`
   - Android клиент меньше подвержен блокировкам YouTube
   
2. **Поддержка cookies из файла и браузера**
   - Файл: `youtube_cookies.txt` (приоритет)
   - Браузер: Chrome/Firefox (fallback)
   
3. **Улучшенное экранирование MarkdownV2**
   - Исправлены ошибки с точками и спецсимволами в названиях
   
4. **Исправлено расширение файлов**
   - Видео: `.mp4` (было `.mp3.mp4`)
   - Аудио: `.mp3`

### 📁 Новые файлы:

- `youtube_cookies.txt` - Файл с cookies YouTube
- `check_cookies.sh` - Проверка работы cookies
- `update_ytdlp.sh` - Обновление yt-dlp
- `run_with_cookies.sh` - Запуск бота с cookies
- `YOUTUBE_COOKIES.md` - Подробная документация
- `QUICKSTART.md` - Быстрый старт

---

## 🚀 Запуск:

### Вариант 1: Автоматический (рекомендуется)
```bash
./run_with_cookies.sh
```

### Вариант 2: Ручной
```bash
export YTDL_COOKIES_FILE=youtube_cookies.txt
cargo run --release
```

---

## 🔍 Проверка работы:

```bash
# Проверить cookies
./check_cookies.sh

# Проверить версию yt-dlp
yt-dlp --version

# Тестовая загрузка
yt-dlp --cookies youtube_cookies.txt \
       --extractor-args "youtube:player_client=android,web" \
       --print "%(title)s" \
       "https://www.youtube.com/watch?v=VIDEO_ID"
```

---

## ⚠️ Если проблемы остались:

### 1. Обнови cookies

Cookies устаревают через 2-4 недели. Обнови их:

```bash
# Способ 1: Через расширение браузера
# Установи: https://chrome.google.com/webstore/detail/get-cookiestxt-locally/cclelndahbckbenkjhflpdbgdldlbecc
# Экспортируй cookies с YouTube → сохрани как youtube_cookies.txt

# Способ 2: Из Firefox (проще)
export YTDL_COOKIES_BROWSER=firefox
./run_with_cookies.sh
```

### 2. Проверь версию yt-dlp

```bash
yt-dlp --version
# Должно быть >= 2024.12.xx

# Если старая версия:
./update_ytdlp.sh
```

### 3. Проверь логи бота

```bash
cargo run --release 2>&1 | grep -E "(ERROR|WARN|cookie|youtube)"
```

---

## 📊 Технические детали:

### Что делает `--extractor-args "youtube:player_client=android,web"`:

1. **Android клиент** - используется в первую очередь
   - Меньше блокировок
   - Лучше работает с ограниченным контентом
   - Не требует signature extraction

2. **Web клиент** - fallback если Android не работает
   - Стандартный веб-интерфейс
   - Может требовать signature extraction

### Порядок использования cookies:

1. `YTDL_COOKIES_FILE` (файл youtube_cookies.txt)
2. `YTDL_COOKIES_BROWSER` (chrome/firefox)
3. Без cookies (может не работать)

---

## 📝 Переменные окружения:

```bash
# Основные
YTDL_COOKIES_FILE=youtube_cookies.txt    # Файл с cookies (приоритет)
YTDL_COOKIES_BROWSER=firefox              # Браузер для извлечения cookies

# Дополнительные
YTDL_BIN=yt-dlp                           # Путь к yt-dlp
TELOXIDE_TOKEN=your_token                 # Токен Telegram бота
```

---

## 🎯 Частые вопросы:

### Q: Видео всё равно не скачивается
**A:** Возможные причины:
- Видео удалено/приватное
- Региональные ограничения
- Возрастные ограничения (требуется авторизация)
- Попробуй другое видео для теста

### Q: Cookies устарели, как обновить?
**A:** См. раздел "Обнови cookies" выше

### Q: Ошибка "keyring module not found"
**A:** Установи зависимости:
```bash
pip3 install keyring pycryptodomex
```

### Q: Нужно ли обновлять yt-dlp каждый раз?
**A:** Рекомендуется обновлять раз в месяц или при ошибках

---

## 📚 Дополнительные ресурсы:

- [YOUTUBE_COOKIES.md](YOUTUBE_COOKIES.md) - Подробная документация по cookies
- [QUICKSTART.md](QUICKSTART.md) - Быстрый старт
- [yt-dlp wiki](https://github.com/yt-dlp/yt-dlp/wiki) - Официальная документация

---

**Готово!** Бот должен работать без ошибок YouTube! 🎉

Если проблемы остались - проверь:
1. ✅ Версия yt-dlp >= 2024.12
2. ✅ Файл youtube_cookies.txt существует
3. ✅ Бот пересобран: `cargo build --release`
4. ✅ Бот запущен с cookies: `./run_with_cookies.sh`

