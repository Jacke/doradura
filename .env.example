# ============================================
# Doradura — Environment Configuration
# ============================================
# Copy to .env and fill in required values.
# Only TELOXIDE_TOKEN is required — everything else has sensible defaults.

# ── Bot (required) ──────────────────────────
TELOXIDE_TOKEN=your_telegram_bot_token_here
# BOT_TOKEN=                       # alias for TELOXIDE_TOKEN

# ── Admin ───────────────────────────────────
ADMIN_USERNAME=your_username        # without @, enables admin commands
ADMIN_USER_ID=123456789             # numeric Telegram ID (get from @userinfobot)
# ADMIN_IDS=123456789,987654321    # comma-separated list for multiple admins

# ── Paths ───────────────────────────────────
# DATABASE_PATH=database.sqlite
# DOWNLOAD_FOLDER=~/downloads/dora-files   # macOS default; ~/downloads on Linux
# TEMP_FILES_DIR=/tmp
# LOG_FILE_PATH=app.log

# ── yt-dlp ──────────────────────────────────
# YTDL_BIN=yt-dlp                  # path or name of yt-dlp binary
# YTDL_COOKIES_FILE=youtube_cookies.txt
# YTDL_COOKIES_BROWSER=            # chrome|firefox|safari|brave|edge (macOS needs Full Disk Access)

# ── Local Bot API (files >50 MB, up to 2 GB) ─
# BOT_API_URL=http://localhost:8081
# API_ID=                          # from https://my.telegram.org/apps
# API_HASH=                        # from https://my.telegram.org/apps

# ── Webhook (default: long polling) ─────────
# WEBHOOK_URL=https://your-domain.com/webhook

# ── Downsub gRPC (summaries & subtitles) ────
# DOWNSUB_GRPC_ENDPOINT=http://localhost:50051

# ── Proxy (required on Railway for YouTube) ─
# WARP_PROXY=socks5://127.0.0.1:1080
# PROXY_FILE=proxies.txt           # one proxy per line
# PROXY_STRATEGY=round_robin       # round_robin | random | weighted | fixed
# PROXY_ROTATION_ENABLED=true
# PROXY_MIN_HEALTH=0.5             # 0.0–1.0, skip proxies below this success rate
# PROXY_UPDATE_URL=                # URL to fetch fresh proxy list
# PROXY_UPDATE_INTERVAL=3600       # seconds between proxy list fetches

# ── Queue ───────────────────────────────────
# QUEUE_MAX_CONCURRENT=2           # parallel downloads (keep low for YouTube)
# QUEUE_INTER_DOWNLOAD_DELAY_MS=3000   # ms between starting new downloads
# QUEUE_CHECK_INTERVAL_MS=100      # ms between queue polling cycles

# ── Subscription pricing (Telegram Stars) ───
# PREMIUM_PRICE_STARS=350          # ~$6/month
# VIP_PRICE_STARS=850              # ~$15/month

# ── Metrics & Monitoring ────────────────────
# METRICS_ENABLED=true
# METRICS_PORT=9090
# PROMETHEUS_URL=http://prometheus:9090

# ── Alerts ──────────────────────────────────
# ALERTS_ENABLED=true
# ALERT_ERROR_RATE_THRESHOLD=5.0   # % error rate to trigger alert
# ALERT_QUEUE_DEPTH_THRESHOLD=50   # queue size to trigger alert
# ALERT_RETRY_RATE_THRESHOLD=30.0  # % retry rate to trigger alert

# ── Analytics ───────────────────────────────
# ANALYTICS_CACHE_UPDATE_INTERVAL=300  # seconds between cache refreshes

# ── Health checks ───────────────────────────
# HEALTH_CHECK_ENABLED=true
# HEALTH_CHECK_INTERVAL_SECS=60

# ── Copyright tag in sent files ─────────────
# BOT_TAG=@DoraDuraDoraDuraBot
# COPYRIGHT_ENABLED=true

# ── MTProto (experimental, for large file downloads) ─
# MTPROTO_SESSION_PATH=mtproto_session.bin
