# Telegram Bot Configuration
TELOXIDE_TOKEN=your_bot_token_here

# yt-dlp Binary Path (optional, defaults to "yt-dlp")
# YTDL_BIN=yt-dlp

# Download Folder Configuration (optional)
# Supports tilde (~) expansion for home directory
# Defaults:
#   - macOS: ~/downloads/dora-files
#   - Other platforms: ~/downloads
# DOWNLOAD_FOLDER=~/downloads/dora-files

# YouTube Cookies Configuration
# Option 1: Use cookies file (RECOMMENDED for production)
# This takes priority over browser cookies
YTDL_COOKIES_FILE=youtube_cookies.txt

# Option 2: Extract cookies from browser (requires keyring & pycryptodomex)
# Supported: chrome, firefox, safari, brave, chromium, edge, opera, vivaldi
# Leave empty or comment out if using cookies file
# YTDL_COOKIES_BROWSER=firefox

# Database Configuration
# DATABASE_URL=database.sqlite

# Admin User IDs (comma-separated)
# ADMIN_IDS=123456789,987654321

# Telegram Mini App Configuration (optional)
# Set these to enable Telegram Mini App functionality

# Port for Mini App web server (leave empty to disable Mini App)
# WEBAPP_PORT=8080

# Public URL for Mini App (must be HTTPS and publicly accessible)
# This URL should point to your server running on WEBAPP_PORT
# For development, you can use ngrok: ngrok http 8080
# WEBAPP_URL=https://your-domain.com

# Example with ngrok for development:
# WEBAPP_PORT=8080
# WEBAPP_URL=https://abc123.ngrok.io

# Bot API Configuration (optional)
# Use this if you're running a local Bot API server
# BOT_API_URL=http://localhost:8081

# Webhook Configuration (optional, uses long polling by default)
# WEBHOOK_URL=https://your-domain.com/webhook
