# Ручной деплой на Railway (Пошаговая инструкция)

Поскольку Railway CLI требует интерактивного режима, вот простой способ задеплоить бота через веб-интерфейс.

## 🚀 Быстрый старт (5 минут)

### Шаг 1: Подготовка репозитория

1. Убедитесь, что все файлы закоммичены:

```bash
git add .
git commit -m "Prepare for Railway deployment"
git push
```

### Шаг 2: Создание проекта на Railway

1. Откройте [railway.app](https://railway.app)
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Выберите репозиторий `doradura`
5. Railway автоматически обнаружит `Dockerfile` и начнет сборку

### Шаг 3: Настройка переменных окружения

После создания проекта:

1. Откройте проект в Railway Dashboard
2. Перейдите в **Variables**
3. Добавьте следующие переменные:

#### Обязательная переменная:

```
TELOXIDE_TOKEN = 6310079371:AAH5D08Tvmt5W7Lo8PDHBL_qxq4Cgv1wpUw
```

#### Рекомендуемые переменные:

```
YTDL_COOKIES_FILE = youtube_cookies.txt
ADMIN_IDS = ваш_telegram_id
```

#### Опциональные переменные для Mini App:

```
WEBAPP_PORT = 8080
WEBAPP_URL = https://ваш-проект.railway.app
```

### Шаг 4: Получение домена

1. В Railway Dashboard перейдите в **Settings**
2. Найдите секцию **Networking**
3. Нажмите **Generate Domain**
4. Скопируйте полученный URL (например: `doradura-bot-production.up.railway.app`)

### Шаг 5: Обновление WEBAPP_URL (если используется Mini App)

Если вы включили Mini App:

1. Вернитесь в **Variables**
2. Обновите `WEBAPP_URL` на полученный домен:
   ```
   WEBAPP_URL = https://doradura-bot-production.up.railway.app
   ```

### Шаг 6: Проверка деплоя

1. Перейдите в **Deployments**
2. Дождитесь завершения сборки (обычно 5-10 минут)
3. Откройте **View Logs** чтобы увидеть логи запуска
4. Ищите сообщение: `Starting bot...`

## 📱 Проверка работы бота

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте команду `/start`
4. Если бот отвечает - всё работает! 🎉

## 🔍 Мониторинг

### Просмотр логов

1. Railway Dashboard → Ваш проект → **Deployments**
2. Нажмите на активный деплой
3. Нажмите **View Logs**

### Полезные логи для поиска

- `Starting bot...` - бот запущается
- `Bot username:` - имя бота определено
- `✅ Cookies configured` - cookies настроены
- `Starting bot in long polling mode` - бот работает в режиме polling

## 🛠️ Настройка постоянного хранилища для БД

Чтобы база данных не терялась при редеплое:

1. Railway Dashboard → **Settings**
2. Найдите **Volumes**
3. Нажмите **Add Volume**
4. Установите:
   - **Mount Path**: `/app`
   - **Size**: 1 GB (достаточно для SQLite)

## 📝 YouTube Cookies (важно для работы с YouTube)

Если у вас есть файл `youtube_cookies.txt`:

### Вариант 1: Добавить в репозиторий (не рекомендуется для публичных репо)

```bash
# Добавьте файл в репозиторий
git add youtube_cookies.txt
git commit -m "Add YouTube cookies"
git push
```

Railway автоматически скопирует файл при деплое.

### Вариант 2: Через переменную окружения (безопаснее)

```bash
# Закодируйте cookies в base64
base64 youtube_cookies.txt > cookies_base64.txt

# Откройте файл и скопируйте содержимое
cat cookies_base64.txt
```

Затем в Railway Dashboard → Variables:
```
YOUTUBE_COOKIES_BASE64 = <содержимое файла cookies_base64.txt>
```

И добавьте в Dockerfile перед CMD:
```dockerfile
RUN if [ ! -z "$YOUTUBE_COOKIES_BASE64" ]; then \
      echo "$YOUTUBE_COOKIES_BASE64" | base64 -d > youtube_cookies.txt; \
    fi
```

### Вариант 3: Использовать браузер для извлечения cookies

В Railway Dashboard → Variables:
```
YTDL_COOKIES_BROWSER = chrome
```

Это будет извлекать cookies из Chrome при каждом запуске.

## 🔄 Обновление бота

### При изменении кода:

```bash
git add .
git commit -m "Update bot"
git push
```

Railway автоматически начнет новый деплой.

### При изменении переменных:

1. Railway Dashboard → Variables
2. Измените нужную переменную
3. Railway автоматически перезапустит сервис

## ⚠️ Troubleshooting

### Бот не отвечает

1. Проверьте логи: Railway Dashboard → View Logs
2. Найдите ошибки (красным цветом)
3. Убедитесь, что `TELOXIDE_TOKEN` правильный

### YouTube downloads не работают

1. Проверьте наличие cookies: `✅ Cookies configured` в логах
2. Если нет, добавьте `YTDL_COOKIES_FILE` или `YTDL_COOKIES_BROWSER`
3. Перезапустите сервис

### Out of Memory

1. Railway Dashboard → Settings
2. Увеличьте Memory Limit
3. Или уменьшите количество одновременных загрузок в коде

### Build failed

1. Проверьте логи сборки
2. Убедитесь, что все файлы на месте (`Dockerfile`, `Cargo.toml`, etc.)
3. Проверьте синтаксис `Dockerfile`

## 💡 Полезные советы

1. **Используйте environment-specific переменные** для разделения dev/prod окружений
2. **Настройте webhooks** вместо long polling для production (быстрее и эффективнее)
3. **Мониторьте логи регулярно** для раннего обнаружения проблем
4. **Делайте бэкапы базы данных** периодически

## 🎯 Next Steps

После успешного деплоя:

1. Протестируйте все основные функции бота
2. Настройте регулярные бэкапы БД
3. Добавьте мониторинг (например, через Sentry)
4. Оптимизируйте настройки на основе логов

---

**Готово!** Ваш бот теперь работает на Railway! 🚀

Если возникнут вопросы, проверьте:
- [Полное руководство](RAILWAY_DEPLOY.md)
- [Быстрый старт](QUICK_DEPLOY.md)
- [Railway Documentation](https://docs.railway.app)
