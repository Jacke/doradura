# 💾 База данных в Git

## Концепция

База данных `database.sqlite` теперь **хранится в git** и автоматически синхронизируется между:
- Локальной машиной (разработка)
- Railway (production)

---

## ✅ Преимущества

1. **Синхронизация данных** - пользователи и настройки одинаковые везде
2. **Версионность** - можно откатиться к предыдущей версии
3. **Бэкапы** - история БД в git
4. **Простота** - не нужно настраивать отдельное хранилище

---

## 📋 Файлы БД

| Файл | В Git? | Описание |
|------|--------|----------|
| `database.sqlite` | ✅ Да | Основная БД |
| `database.sqlite-shm` | ❌ Нет | Shared memory (временный) |
| `database.sqlite-wal` | ❌ Нет | Write-Ahead Log (временный) |

---

## 🔄 Рабочий процесс

### Локальная разработка

1. **Внесите изменения в БД** (добавьте пользователей, настройте что-то)
2. **Закоммитьте БД:**
   ```bash
   git add database.sqlite
   git commit -m "chore: update database with new users"
   git push
   ```
3. **Railway автоматически получит обновлённую БД**

### На Railway

1. **Railway деплоит новый код**
2. **Dockerfile копирует `database.sqlite` из git**
3. **Бот использует актуальную версию БД**

---

## 📝 Примеры использования

### Добавить тестового пользователя локально

```bash
# 1. Запустите бота локально
cargo run

# 2. В Telegram отправьте /start боту
# 3. Пользователь добавится в БД

# 4. Закоммитьте
git add database.sqlite
git commit -m "chore: add test user to database"
git push

# 5. Railway получит обновлённую БД при следующем деплое
```

### Обновить настройки пользователя

```bash
# 1. Измените настройки через бота (формат, качество, etc)
# 2. Закоммитьте изменения
git add database.sqlite
git commit -m "chore: update user preferences"
git push
```

### Синхронизировать БД с Railway

```bash
# Pull изменения если они были на Railway
git pull

# Push локальные изменения
git push
```

---

## ⚠️ Важные замечания

### 1. Размер файла

SQLite файл растёт со временем. Текущий размер: **147KB**

**Рекомендации:**
- ✅ До 10MB - нормально для git
- ⚠️ 10MB - 50MB - приемлемо, но следите
- ❌ Больше 50MB - рассмотрите альтернативы

**Проверка размера:**
```bash
ls -lh database.sqlite
```

**Очистка старых данных:**
```bash
# Удалить старые записи истории
sqlite3 database.sqlite "DELETE FROM download_history WHERE downloaded_at < datetime('now', '-30 days')"

# Оптимизировать БД
sqlite3 database.sqlite "VACUUM"
```

### 2. Конфликты слияния

Если БД меняется одновременно локально и на Railway:

```bash
# При git pull может возникнуть конфликт
git pull
# CONFLICT (content): Merge conflict in database.sqlite

# Решение 1: Использовать локальную версию
git checkout --ours database.sqlite
git add database.sqlite
git commit

# Решение 2: Использовать удалённую версию
git checkout --theirs database.sqlite
git add database.sqlite
git commit

# Решение 3: Экспорт/импорт данных (см. ниже)
```

### 3. Бинарный файл

SQLite - бинарный файл, git diff не покажет изменения читабельно.

**Просмотр содержимого:**
```bash
# Показать таблицы
sqlite3 database.sqlite ".tables"

# Показать пользователей
sqlite3 database.sqlite "SELECT * FROM users"

# Экспорт в SQL
sqlite3 database.sqlite .dump > database_backup.sql
```

---

## 🔧 Утилиты

### Экспорт БД в SQL

```bash
sqlite3 database.sqlite .dump > database.sql
git add database.sql
git commit -m "chore: export database to SQL"
```

### Импорт SQL в БД

```bash
sqlite3 database.sqlite < database.sql
```

### Создать бэкап

```bash
# С датой
cp database.sqlite "backups/database_$(date +%Y%m%d_%H%M%S).sqlite"

# Или экспорт в SQL
sqlite3 database.sqlite .dump > "backups/database_$(date +%Y%m%d_%H%M%S).sql"
```

### Проверить схему

```bash
sqlite3 database.sqlite .schema
```

---

## 🚀 Деплой с обновлённой БД

### Автоматический (рекомендуется)

```bash
# 1. Обновите БД локально
# 2. Закоммитьте
git add database.sqlite
git commit -m "chore: update database"
git push

# 3. Railway автоматически задеплоит обновлённую БД
```

### Ручной

Если нужно быстро обновить БД на Railway:

```bash
# 1. Экспортируйте БД
sqlite3 database.sqlite .dump > temp_db.sql

# 2. Загрузите на Railway через shell
railway run sqlite3 /app/database.sqlite < temp_db.sql

# 3. Перезапустите
railway restart
```

---

## 📊 Мониторинг размера БД

### Локально

```bash
# Размер файла
du -h database.sqlite

# Количество пользователей
sqlite3 database.sqlite "SELECT COUNT(*) FROM users"

# Количество записей в истории
sqlite3 database.sqlite "SELECT COUNT(*) FROM download_history"
```

### На Railway

Через логи при старте:
```
[INFO] Database size: 147KB
[INFO] Total users: 5
[INFO] Download history entries: 120
```

---

## 🔒 Безопасность

### Что НЕ хранить в БД в git

- ❌ Пароли пользователей (если добавите авторизацию)
- ❌ Платёжные данные
- ❌ Секретные ключи

### Что можно хранить

- ✅ Telegram User IDs (публичные)
- ✅ Настройки пользователей
- ✅ История загрузок (если не конфиденциальная)

---

## 📈 Если БД станет большой

### Вариант 1: Git LFS (Large File Storage)

```bash
# Установите git-lfs
brew install git-lfs

# Настройте для БД
git lfs track "*.sqlite"
git add .gitattributes
git commit -m "chore: use git-lfs for database"
```

### Вариант 2: Railway Volumes

Используйте постоянное хранилище Railway:

1. Railway Dashboard → Settings → Volumes
2. Add Volume
3. Mount path: `/app/data`
4. Обновите код для использования `/app/data/database.sqlite`

### Вариант 3: Внешняя БД

Для production рассмотрите:
- PostgreSQL (Railway Postgres plugin)
- MySQL
- External managed database

---

## ✅ Текущий статус

- **Размер БД:** 147KB
- **В git:** ✅ Да
- **Auto-sync:** ✅ Да
- **Бэкапы:** ✅ Через git history

---

## 🎯 Best Practices

1. **Коммитьте БД регулярно:**
   ```bash
   git add database.sqlite
   git commit -m "chore: update database"
   git push
   ```

2. **Делайте описательные коммиты:**
   ```bash
   # ✅ Хорошо
   git commit -m "chore: add 3 new users, update user123 preferences"

   # ❌ Плохо
   git commit -m "update db"
   ```

3. **Очищайте старые данные:**
   ```bash
   # Раз в месяц
   sqlite3 database.sqlite "DELETE FROM download_history WHERE downloaded_at < datetime('now', '-30 days')"
   sqlite3 database.sqlite "VACUUM"
   ```

4. **Следите за размером:**
   ```bash
   # Если больше 10MB
   ls -lh database.sqlite
   ```

---

**База данных теперь под версионным контролем!** 🎉
