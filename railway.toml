# Railway deployment configuration
# Ensures Dockerfile.combined is used for multi-service setup
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.s6"

[deploy]
numReplicas = 1
sleepApplication = false
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
