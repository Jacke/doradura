# 🔍 Debug Railway Environment Variables

## Проблема
Переменные добавлены в Railway Dashboard, но бот их не видит:
```
⚠️  YTDL_COOKIES_FILE: not set
⚠️  YTDL_COOKIES_BROWSER: not set
```

---

## ✅ Checklist - Проверьте каждый пункт!

### 1. Правильный Service
Railway может иметь несколько сервисов в проекте.

**Проверьте:**
1. Railway Dashboard → Ваш проект
2. Убедитесь, что переменные добавлены в **тот же сервис**, который запущен
3. Обычно это сервис с названием из GitHub репозитория

**Как проверить:**
- Слева в списке сервисов должен быть только ОДИН активный
- Переменные должны быть именно в НЁМ

### 2. Правильный Environment
Railway может иметь несколько environments (production, staging, etc.)

**Проверьте:**
1. Вверху справа выберите Environment
2. По умолчанию это **"production"**
3. Убедитесь, что переменные добавлены в активный environment

### 3. Переменные написаны правильно

**Проверьте в Variables:**

Должно быть ТОЧНО так (без кавычек в имени и значении):

| Variable Name | Variable Value | ✅/❌ |
|--------------|----------------|------|
| `TELOXIDE_TOKEN` | `6310079371:AAH5D08Tvmt5W7Lo8PDHBL_qxq4Cgv1wpUw` | |
| `YTDL_COOKIES_FILE` | `youtube_cookies.txt` | |

**НЕ ДОЛЖНО БЫТЬ:**
- ❌ Кавычек в имени: `"TELOXIDE_TOKEN"`
- ❌ Кавычек в значении: `"youtube_cookies.txt"`
- ❌ Пробелов в начале или конце
- ❌ Переносов строк

### 4. Deployment успешный

**Проверьте:**
1. Deployments → Latest deployment
2. Статус должен быть **"Success"** ✅ (зеленый)
3. Если статус **"Failed"** ❌ или **"Crashed"** - смотрите логи

### 5. Service перезапущен

После добавления переменных Railway ДОЛЖЕН автоматически перезапустить сервис.

**Если этого не произошло:**
1. Settings → **"Restart"**
2. Или Deployments → **"Redeploy"**

---

## 🔧 Принудительный перезапуск

### Способ 1: Через Dashboard

1. Railway Dashboard → Ваш проект
2. Settings (слева в меню)
3. Прокрутите вниз
4. Нажмите **"Restart Deployment"**

### Способ 2: Через CLI

```bash
railway restart
```

### Способ 3: Redeploy

1. Deployments
2. Последний deployment
3. Три точки (⋮)
4. **"Redeploy"**

---

## 📊 Как проверить что переменные установлены

### В Railway Logs:

Добавьте временный debug код или посмотрите логи старта:

Логи должны показывать:
```
[INFO] Starting bot...
[INFO] Environment variables:
       TELOXIDE_TOKEN: ***...wpUw (last 5 chars)
       YTDL_COOKIES_FILE: youtube_cookies.txt
```

Если показывает `not set` - переменные НЕ установлены для этого сервиса!

---

## 🎯 Пошаговая проверка

### Шаг 1: Откройте Railway Dashboard
https://railway.app → Ваш проект

### Шаг 2: Проверьте сервис
- Слева должен быть список сервисов
- Откройте тот, который деплоится из GitHub
- Обычно называется по имени репозитория

### Шаг 3: Проверьте Environment
- Вверху справа: должно быть `production`
- Если нет - переключите

### Шаг 4: Проверьте Variables
Variables (вкладка слева) → должны быть:
```
TELOXIDE_TOKEN=6310079371:AAH5D08Tvmt5W7Lo8PDHBL_qxq4Cgv1wpUw
YTDL_COOKIES_FILE=youtube_cookies.txt
```

### Шаг 5: Перезапустите
Settings → Restart Deployment

### Шаг 6: Проверьте логи
Deployments → View Logs

Должны увидеть через 1-2 минуты:
```
✅ YTDL_COOKIES_FILE: /app/youtube_cookies.txt
✅ Starting bot in long polling mode
```

---

## 🚨 Если ВСЁТАКИ не работает

### Проверка 1: Правильный ли это сервис?

```bash
# В Railway Dashboard
# Deployments → View Logs
#
# В самом начале логов должно быть:
# ✓ Building...
# ✓ FROM rust:1.83-slim as builder
# ✓ COPY Cargo.toml Cargo.lock ./
# ...
```

Если этого НЕТ - вы смотрите не на тот сервис!

### Проверка 2: А точно variables добавлены?

1. Variables → должен быть список
2. Если пусто - добавьте заново!

### Проверка 3: Screenshot

Сделайте скриншот Railway Dashboard → Variables и отправьте в issue.

---

## 💡 Временное решение для теста

Если ничего не помогает, попробуйте добавить переменные через `railway.toml`:

### 1. Создайте файл railway.toml в корне проекта:

```toml
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
numReplicas = 1
sleepApplication = false
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# ВНИМАНИЕ: НЕ РЕКОМЕНДУЕТСЯ для production!
# Используйте только для теста
[env]
TELOXIDE_TOKEN = "6310079371:AAH5D08Tvmt5W7Lo8PDHBL_qxq4Cgv1wpUw"
YTDL_COOKIES_FILE = "youtube_cookies.txt"
```

### 2. Закоммитьте и запушьте:

```bash
git add railway.toml
git commit -m "temp: add env vars to railway.toml for testing"
git push
```

### 3. Проверьте через 2-3 минуты

**ВАЖНО:** После того как заработает, УДАЛИТЕ токен из railway.toml и используйте Dashboard!

---

## ✅ Контрольный список

- [ ] Переменные добавлены в правильный Service
- [ ] Переменные добавлены в правильный Environment (production)
- [ ] Имена переменных БЕЗ кавычек
- [ ] Значения БЕЗ кавычек
- [ ] Сервис перезапущен (Restart Deployment)
- [ ] Deployment успешный (Status: Success)
- [ ] Логи проверены (View Logs)
- [ ] Прошло 2-3 минуты после перезапуска

---

## 📞 Всё ещё не работает?

1. **Сделайте скриншот:**
   - Railway Dashboard → Variables (весь экран)
   - Railway Dashboard → Deployments → Logs (первые 50 строк)

2. **Проверьте версию Railway:**
   ```bash
   railway --version
   ```

3. **Попробуйте создать новый проект:**
   - Railway → New Project
   - Deploy from GitHub repo
   - Выберите репозиторий заново
   - Добавьте переменные

---

**Проблема ДОЛЖНА решиться! Следуйте чеклисту пошагово!** 🔍
