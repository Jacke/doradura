# Staging Environment Configuration
# For testing new features before production deployment

# Telegram Bot Token (staging bot)
TELOXIDE_TOKEN=8224275354:AAFB__G9OXj6UWKbDMFmv0mSD8zjk4Jzivw

# yt-dlp Binary Path (optional, defaults to "yt-dlp")
# YTDL_BIN=yt-dlp

# Download Folder Configuration (optional)
# Use separate folder for staging to avoid conflicts
DOWNLOAD_FOLDER=~/downloads/dora-staging

# YouTube Cookies Configuration
# Option 1: Use cookies file (RECOMMENDED for production)
YTDL_COOKIES_FILE=youtube_cookies.txt

# Option 2: Extract cookies from browser (for development)
# YTDL_COOKIES_BROWSER=chrome

# Database Configuration (separate from production)
DATABASE_URL=database_staging.sqlite

# Admin User IDs (comma-separated)
# ADMIN_IDS=123456789,987654321

# Telegram Mini App Configuration (optional)
# Use different port than production
WEBAPP_PORT=8081
# WEBAPP_URL=https://staging-domain.com

# Bot API Configuration (optional)
# BOT_API_URL=http://localhost:8081

# Webhook Configuration (optional, uses long polling by default)
# WEBHOOK_URL=https://staging-domain.com/webhook

# Logging
RUST_LOG=debug
